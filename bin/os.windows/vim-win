#!/bin/bash

BIN_FILE="$SYS_PROG32_DIR/Vim/vim81/vim.exe"

if [ ! -x "$BIN_FILE" ]; then
  echo "no $BIN_FILE"
  exit
fi

exec winpty "$BIN_FILE" -u $USERPROFILE/_vimrc "$@"
#exec winpty "$BIN_FILE" -u $USERPROFILE/_vimrc "$@" > /dev/null 2>&1 &
