#!/bin/sh

BIN_DIR="$PROG_FILES_32_DIR/FastStone Image Viewer"
BIN_FILE="fsviewer.exe"

if [ ! -x "$BIN_DIR/$BIN_FILE" ]; then
  echo "no $BIN_DIR/$BIN_FILE"
  exit
fi

PATH="$BIN_DIR:$PATH"

exec "$BIN_FILE" "$@"

