#!/bin/sh

BIN_DIR="$PROG_FILES_32_DIR/Google/Chrome/Application"
BIN_FILE="chrome.exe"

if [ ! -x "$BIN_DIR/$BIN_FILE" ]; then
  echo "no $BIN_DIR/$BIN_FILE"
  exit
fi

PATH="$BIN_DIR:$PATH"

exec "$BIN_FILE" "$@"

