# gitconfig

[user]
  name = username
  email = username@example.com
[core]
  autocrlf = input
  quotepath = false
  pager = less
  editor = vim.sh
  excludesfile = ~/.gitignore_global

[diff]
  tool = vimdiff.sh
[merge]
  tool = vimdiff.sh
[difftool]
  prompt = false
[mergetool]
  prompt = false

[alias]
  graph     = log --graph --pretty=format:'%C(red)%h %C(green)%cd %C(blue)%an %C(yellow)%d %C(reset)%s' --date=format-local:'%Y/%m/%d %H:%M'
  graph-all = log --graph --pretty=format:'%C(red)%h %C(green)%cd %C(blue)%an %C(yellow)%d %C(reset)%s' --date=format-local:'%Y/%m/%d %H:%M' --all
  co = checkout
  dd = difftool -d

## wsl
#[credential]
#  helper = /mnt/c/Program\\ Files/Git/mingw64/bin/git-credential-manager-core.exe

## gitbash/msys2
#[credential]
#  helper = manager-core
